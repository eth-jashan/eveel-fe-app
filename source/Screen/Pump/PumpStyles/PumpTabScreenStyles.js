import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  screen: { flex: 1, justifyContent: "center", backgroundColor: "black" },
  title: { alignSelf: "center" },
});

export default styles;
